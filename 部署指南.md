# 设备图书馆 - 上线部署指南

本指南面向首次上线,覆盖从一台全新云服务器到系统可访问的完整流程。推荐使用 Docker 方案(方式一),零环境依赖;无法用 Docker 时参考方式二手动部署。

## 部署架构

```
用户浏览器
    │  http://服务器IP:8093 (8093)
    ▼
┌─────────────────┐
│ ioedu-frontend   │  Nginx:托管前端静态资源
│ (Nginx 容器)     │  /api 反向代理 → backend:8080
└────────┬────────┘
         ▼
┌─────────────────┐      ┌─────────────────┐
│ ioedu-backend    │ ───→ │ ioedu-mysql      │
│ (Spring Boot)    │ 3306 │ (MySQL 8,数据卷) │
└─────────────────┘      └─────────────────┘
```

三个容器由 `docker-compose.yml` 统一编排,数据库数据存放在 Docker 数据卷 `ioedu-mysql-data` 中,容器重建不丢数据。

## 服务器要求

| 项 | 最低 | 推荐 |
|---|---|---|
| CPU / 内存 | 1核2G | 2核4G |
| 磁盘 | 20G | 40G |
| 系统 | Ubuntu 20.04+ / CentOS 7+ / Debian 11+ | Ubuntu 22.04 |
| 需开放端口 | 8093(HTTP) | 8093,配 HTTPS 另需 80 + 443 |

云厂商安全组记得放行 8093 端口(配 HTTPS 还需 80 和 443)。3306、8080 不需要对外开放。

---

# 方式一:Docker 一键部署(推荐)

## 1. 安装 Docker

Ubuntu / Debian:

```bash
curl -fsSL https://get.docker.com | bash
sudo systemctl enable --now docker
docker --version && docker compose version
```

CentOS:

```bash
curl -fsSL https://get.docker.com | bash -s docker --mirror Aliyun
sudo systemctl enable --now docker
```

国内服务器建议配置镜像加速(阿里云容器镜像服务控制台可获取专属加速地址):

```bash
sudo mkdir -p /etc/docker
sudo tee /etc/docker/daemon.json <<'EOF'
{ "registry-mirrors": ["https://docker.m.daocloud.io", "https://dockerproxy.com"] }
EOF
sudo systemctl restart docker
```

## 2. 上传项目代码

有 Git 仓库:

```bash
cd /opt && git clone <你的仓库地址> IOEDU-New && cd IOEDU-New
```

没有仓库就本地打包上传(在 Windows 本机执行,注意排除依赖目录):

```powershell
# 本机 PowerShell,在 IOEDU-New 上级目录执行
Compress-Archive -Path IOEDU-New -DestinationPath ioedu.zip -Force
scp ioedu.zip root@服务器IP:/opt/
```

```bash
# 服务器上
cd /opt && unzip ioedu.zip && cd IOEDU-New
# 如误传了本地构建产物,清理掉(不影响部署,Docker 内会重新构建)
rm -rf frontend/node_modules frontend/dist target IoeduFront
```

## 3. 配置生产密码

在项目根目录创建 `.env` 文件(不建议生产沿用默认密码 123456789):

```bash
cat > .env <<'EOF'
DB_PASSWORD=换成你的强密码
EOF
```

该密码同时用于 MySQL root 和后端连接,由 compose 自动注入,无需改任何代码文件。

## 4. 构建并启动

```bash
docker compose up -d --build
```

首次构建需下载 Maven/npm 依赖,约 5~15 分钟(取决于网络;国内慢可编辑 `frontend/Dockerfile` 取消 npmmirror 注释)。完成后:

```bash
docker compose ps          # 三个容器都应为 running / healthy
docker compose logs -f backend   # 看到 "Started IoeduNewApplication" 即启动成功
curl http://localhost:8093/api/public/stats   # 应返回 {"code":0,...}
```

浏览器访问 `http://服务器IP:8093` 即可看到登录页。

## 5. 初始账号

首次启动自动初始化种子数据:

| 角色 | 账号 | 密码 |
|---|---|---|
| 管理员 | admin@ioedu.cn | admin123 |
| 演示学生 | zhang@stu.ioedu.cn | 123456 |

**上线后请立即登录管理员账号,在用户管理中处理演示账号,并考虑改掉默认密码(可在数据库 users 表更新,或注册新管理员后禁用默认号)。**

## 6. 日常运维

```bash
# 查看状态 / 日志
docker compose ps
docker compose logs -f backend
docker compose logs -f frontend

# 重启某个服务
docker compose restart backend

# 更新发版(拉新代码后重建)
git pull
docker compose up -d --build

# 完全停止(不删数据)
docker compose down

# 数据库备份(生成 SQL 文件,建议配 crontab 每日执行)
docker exec ioedu-mysql sh -c 'exec mysqldump -uroot -p"$MYSQL_ROOT_PASSWORD" ioedu' > backup_$(date +%F).sql

# 数据库恢复
cat backup_2026-07-24.sql | docker exec -i ioedu-mysql sh -c 'exec mysql -uroot -p"$MYSQL_ROOT_PASSWORD" ioedu'
```

## 7. 绑定域名 + HTTPS(可选)

域名解析 A 记录指到服务器 IP 后,推荐用 [Caddy](https://caddyserver.com) 自动签发证书,最少改动:

1. frontend 的端口映射已是 `"8093:80"`,80/443 空闲,可直接给 Caddy 使用
2. 服务器安装 Caddy 并写 `/etc/caddy/Caddyfile`:

```
你的域名.com {
    reverse_proxy localhost:8093
}
```

3. `systemctl restart caddy`,访问 `https://你的域名.com`,证书自动签发续期。

也可以用宝塔面板 / 手动 certbot + nginx,原理相同:HTTPS 终止在外层,反代到 frontend 容器。

---

# 方式二:无 Docker 手动部署

适合内网机器或不方便装 Docker 的场景。

## 1. 环境准备

- JDK 8、MySQL 8、Nginx、Node 18+(仅构建用,可在本机构建后只传产物)

## 2. 数据库

```sql
CREATE DATABASE ioedu DEFAULT CHARACTER SET utf8mb4;
```

## 3. 后端

```bash
# 构建(本机或服务器)
mvn package -Dmaven.test.skip=true
# 产物: target/IOEDU-New-0.0.1-SNAPSHOT.jar

# 服务器上运行(密码用环境变量注入)
export IOEDU_DB_PASSWORD=你的密码
nohup java -jar IOEDU-New-0.0.1-SNAPSHOT.jar > ioedu.log 2>&1 &
```

建议注册成 systemd 服务实现开机自启:

```ini
# /etc/systemd/system/ioedu.service
[Unit]
Description=IOEDU Backend
After=mysql.service

[Service]
Environment=IOEDU_DB_PASSWORD=你的密码
ExecStart=/usr/bin/java -jar /opt/ioedu/IOEDU-New-0.0.1-SNAPSHOT.jar
Restart=always

[Install]
WantedBy=multi-user.target
```

```bash
systemctl daemon-reload && systemctl enable --now ioedu
```

## 4. 前端

```bash
cd frontend && npm install && npm run build
# 把 dist/ 上传到服务器,例如 /opt/ioedu/dist
```

Nginx 配置(`/etc/nginx/conf.d/ioedu.conf`):

```nginx
server {
    listen 80;
    server_name _;

    location /api/ {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location / {
        root /opt/ioedu/dist;
        index index.html;
        try_files $uri $uri/ /index.html;   # SPA 路由回退,必须有
    }
}
```

```bash
nginx -t && systemctl reload nginx
```

---

# 常见问题

| 现象 | 排查 |
|---|---|
| backend 容器反复重启 | `docker compose logs backend`;多为 MySQL 未就绪(healthcheck 会自动等)或 .env 密码与已有数据卷密码不一致。改过密码需 `docker compose down -v` 清卷重建(会清空数据!) |
| 前端构建卡在 npm install | 编辑 `frontend/Dockerfile`,取消 npmmirror 注释 |
| 8093 端口被占用 | `docker-compose.yml` 中 frontend 端口改为如 `"8094:80"`,访问 http://IP:8094 |
| 页面能开但接口 502 | backend 未启动完成,等 healthcheck 通过;`curl http://localhost:8093/api/public/stats` 验证 |
| 刷新子页面 404 | Nginx 缺 `try_files ... /index.html`(Docker 方案已内置) |
| 忘记管理员密码 | 数据库执行:`UPDATE users SET password_hash='$2a$10$N9qo8uLOickgx2ZMRZoMyeIjZAgcfl7p92ldGxad68LJZdL17lhWy' WHERE email='admin@ioedu.cn';`(重置为 `secret`,登录后尽快修改) |

---

# 自动部署(GitHub Actions)

push 到 main 分支时,`.github/workflows/deploy.yml` 会通过 SSH 登录服务器执行 `git pull` + `docker compose up -d --build`(也可在 Actions 页面手动触发)。启用前提:

1. 服务器已按方式一完成首次部署,代码位于 `/opt/ioedu`(即 `cd /opt && git clone <仓库地址> ioedu`),且该目录能免交互执行 `git pull`
2. 在 GitHub 仓库 Settings → Secrets and variables → Actions 中配置三个 Secrets:
   - `SERVER_HOST`:服务器 IP 或域名
   - `SERVER_USER`:SSH 登录用户(如 root)
   - `SERVER_SSH_KEY`:该用户的 SSH 私钥(对应公钥需在服务器 `~/.ssh/authorized_keys` 中)
