# 设备图书馆 - 上线部署指南

本指南面向首次上线,覆盖从一台全新云服务器到系统可访问的完整流程。推荐使用 Docker 方案(方式一),零环境依赖;无法用 Docker 时参考方式二手动部署。

## 部署架构

```
用户浏览器
    │  http://服务器IP:8093 (8093)
    ▼
┌─────────────────┐
│ ioedu-frontend   │  Nginx:托管前端静态资源
│ (Nginx 容器)     │  /api 反向代理 → backend:8080
└────────┬────────┘
         ▼
┌─────────────────┐      ┌─────────────────┐
│ ioedu-backend    │ ───→ │ ioedu-mysql      │
│ (Spring Boot)    │ 3306 │ (MySQL 8,数据卷) │
└─────────────────┘      └─────────────────┘
```

容器由 `docker-compose.yml` 统一编排(另有一个 `ioedu-hub` 项目商店容器,见 5.3),数据库数据存放在 Docker 数据卷 `ioedu-mysql-data` 中,容器重建不丢数据。

## 服务器要求

| 项 | 最低 | 推荐 |
|---|---|---|
| CPU / 内存 | 1核2G | 2核4G |
| 磁盘 | 20G | 40G |
| 系统 | Ubuntu 20.04+ / CentOS 7+ / Debian 11+ | Ubuntu 22.04 |
| 需开放端口 | 8093(HTTP) | 8093,配 HTTPS 另需 80 + 443 |

云厂商安全组记得放行 8093 端口(配 HTTPS 还需 80 和 443)。3306、8080 不需要对外开放。

---

# 方式一:Docker 一键部署(推荐)

## 1. 安装 Docker

Ubuntu / Debian:

```bash
curl -fsSL https://get.docker.com | bash
sudo systemctl enable --now docker
docker --version && docker compose version
```

CentOS:

```bash
curl -fsSL https://get.docker.com | bash -s docker --mirror Aliyun
sudo systemctl enable --now docker
```

国内服务器建议配置镜像加速(阿里云容器镜像服务控制台可获取专属加速地址):

```bash
sudo mkdir -p /etc/docker
sudo tee /etc/docker/daemon.json <<'EOF'
{ "registry-mirrors": ["https://docker.m.daocloud.io", "https://dockerproxy.com"] }
EOF
sudo systemctl restart docker
```

## 2. 上传项目代码

有 Git 仓库:

```bash
cd /opt && git clone <你的仓库地址> IOEDU-New && cd IOEDU-New
```

没有仓库就本地打包上传(在 Windows 本机执行,注意排除依赖目录):

```powershell
# 本机 PowerShell,在 IOEDU-New 上级目录执行
Compress-Archive -Path IOEDU-New -DestinationPath ioedu.zip -Force
scp ioedu.zip root@服务器IP:/opt/
```

```bash
# 服务器上
cd /opt && unzip ioedu.zip && cd IOEDU-New
# 如误传了本地构建产物,清理掉(不影响部署,Docker 内会重新构建)
rm -rf frontend/node_modules frontend/dist target IoeduFront
```

## 3. 配置生产密钥

在项目根目录复制 `.env.example` 为 `.env` 并填写。**`JWT_SECRET` 为必填**(不填 compose 会直接报错退出),其余按需:

```bash
cp .env.example .env
# 生成一个随机 JWT 密钥填进去
echo "JWT_SECRET=$(openssl rand -base64 48)" >> .env
vi .env    # 填 DB_PASSWORD、ADMIN_PASSWORD(可留空随机生成)、AI_API_KEY 等
```

| 变量 | 说明 |
|---|---|
| `DB_PASSWORD` | MySQL root 密码,同时用于后端连接 |
| `JWT_SECRET` | 登录令牌签名密钥,不少于 32 个随机字符;**每套环境必须不同**,泄露需轮换(所有用户重新登录) |
| `ADMIN_EMAIL` / `ADMIN_PASSWORD` | 首次启动创建的管理员;密码留空则随机生成并打印到后端日志一次 |
| `SEED_DEMO` | 是否写入演示师生账号与演示项目/设备,生产保持 `false` |
| `WECHAT_APPID` / `WECHAT_SECRET` | 微信小程序密钥,不用小程序留空 |
| `AI_*` | 大模型接口,可留空后在管理后台在线配置 |

所有密钥只存在 `.env`(已被 git 忽略)和容器环境变量里,不要写进代码文件。

## 4. 构建并启动

```bash
docker compose up -d --build
```

首次构建需下载 Maven/npm 依赖,约 5~15 分钟(取决于网络;国内慢可编辑 `frontend/Dockerfile` 取消 npmmirror 注释)。完成后:

```bash
docker compose ps          # 三个容器都应为 running / healthy
docker compose logs -f backend   # 看到 "Started IoeduNewApplication" 即启动成功
curl http://localhost:8093/api/public/stats   # 应返回 {"code":0,...}
```

浏览器访问 `http://服务器IP:8093` 即可看到登录页。

## 5. 初始账号

首次启动(用户表为空)只创建一个管理员,账号为 `.env` 中的 `ADMIN_EMAIL`(默认 `admin@ioedu.cn`):

- 填了 `ADMIN_PASSWORD` → 就是该密码;
- 没填 → 随机生成 16 位密码并以 WARN 打印到日志一次,用下面命令查看后**立即登录修改**:

```bash
docker compose logs backend | grep "随机初始密码"
```

生产环境不会写入任何演示账号;只有 `SEED_DEMO=true` 时才有演示学生 `zhang@stu.ioedu.cn / 123456` 与四位演示教师(密码 123456),仅用于本地联调和演示。

## 5.1 数据库表结构由 Flyway 管理

表结构脚本在 `src/main/resources/db/migration/`(`V1__baseline.sql` 为基线,之后每次改表新增 `V{n}__说明.sql`),后端启动时会对**每一个租户库**执行未应用的脚本,Hibernate 不再自动改表。已有的老库首次升级到本版本会被自动标记为基线 V1,无需手工处理。平台库(租户注册表)的脚本在 `db/platform/`。

## 5.2 多租户:一套后端服务 100 个客户

后端是**单进程多租户**:每个客户一个独立 MySQL 库(`ioedu_<编码>`),共用一个连接池,按请求的 Host 决定切到哪个库;客户之间数据、登录令牌、上传文件三层隔离。老部署里的 `ioedu` 库自动成为「默认租户」,数据零迁移。

```
浏览器 c001.labcloud.com.cn ─┐
浏览器 c002.labcloud.com.cn ─┼─→ Nginx(同一个前端静态站)─→ backend:8080 ─→ 按 Host 切库:ioedu_c001 / ioedu_c002 / ...
浏览器 www.labcloud.com.cn  ─┘                                              └─→ 默认租户 → ioedu
```

**1)DNS(先 HTTP,HTTPS 可选)**:把根域和泛域名都解析到服务器即可,例如阿里云:

| 主机记录 | 类型 | 记录值 |
|---|---|---|
| `@` | A | 服务器公网 IP |
| `www` | A | 服务器公网 IP |
| `*` | A | 服务器公网 IP |

`*` 一条就能覆盖所有客户子域名(`test-001.labcloud.com.cn` 等),不必每个客户单独申请。暂不配 HTTPS 时,客户访问 `http://编码.labcloud.com.cn:8093`,`.env` 里写:

```
SITE_URL_TEMPLATE=http://{code}.labcloud.com.cn:8093
```

以后要上 HTTPS,再签发**泛域名证书**(Let's Encrypt 只能 DNS-01,HTTP 验证不支持泛域名),把 Nginx 的 `server_name` 改为 `labcloud.com.cn *.labcloud.com.cn`。

**2)`.env` 配置**:

```bash
TENANT_BASE_DOMAIN=labcloud.com.cn        # 子域名前缀即租户编码
TENANT_STRICT_HOST=false                  # 用 IP 访问时仍落到默认租户;全部客户都走域名后可改 true
PLATFORM_TOKEN=$(openssl rand -hex 24)    # 平台管理接口令牌
```

**3)开通客户**:推荐在商店平台管理端操作——登录 `http://www.根域:8093/platform/login`(若已配 HTTPS 则用 8443) → 「客户站点」→「开通新客户」,填编码(子域名前缀)、名称、管理员邮箱即可;系统会建库、初始化管理员、在商店登记该客户并把接入密钥写进新站点,结果页一次性显示管理员初始密码。同一页面还能停用/恢复、重签商店密钥、注销站点。

**配额与到期**:平台控制台「客户站点 → 配额」可给每个站点设置用户数上限、存储上限(MB)与到期日,列表里直接看每站的用户数 / 7 天活跃 / 存储占用 / 本月 AI 用量。到期后该站点所有请求返回 403,续期即恢复;用户数与存储到上限时新建账号 / 上传会被拒绝并提示联系平台。**大模型由各站点自己在「AI 设置」里配置 Key,平台不做 AI 额度限制**;站点可自定"每人每日次数上限"防刷。

**硬件设计助手**(`kicad-ai/`,独立 Python 服务,KiCad 内核):随 `docker compose up -d --build` 一起构建(基础镜像 `kicad/kicad:10.0.5` 较大,首次构建约 10~20 分钟),挂在同域 `/hw/` 子路径(旧地址 `/kicad/` 自动跳转)。学生 / 老师从侧栏「硬件设计助手」进入,直接复用本站登录态,不用再注册;用户、工程、对话按站点隔离,各站管理员在「硬件助手管理」只看本站用户与用量,系统设置只有默认站点(平台)的管理员能改。**每个站点的助手使用该站点自己在「AI 设置」里配置的大模型与 Key**,没配的站点会提示管理员去配置,不会使用平台的 Key。数据在卷 `ioedu-kicad-data`,已纳入每日备份。该服务不可用时只有 `/hw/` 返回 502,主站不受影响。

**自动备份**:服务器已安装 `/opt/ioedu/backup.sh` 与 crontab(每天 03:30),备份全部数据库、上传文件卷与商店资产卷到 `/opt/ioedu-backups/daily-YYYY-mm-dd.*`,保留 14 天;日志在 `backup.log`。手动执行 `bash /opt/ioedu/backup.sh` 即可立即备份。

每个客户站点的数据完全独立:站点标题 / 副标题 / LOGO / 底部信息 / 联系邮箱与服务时间(管理后台「站点设置」)、项目与设备分类、AI 模型配置、商店接入密钥、用户与业务数据都只属于该站点,一个站点改 LOGO 不会影响其他站点。前端代码是所有站点共用的一份,但页面上的品牌信息都从当前站点的设置读取。

命令行方式等价(`code` 只能小写字母数字与短横线):

```bash
source .env
curl -X POST https://www.labcloud.com.cn/api/platform/tenants \
  -H "Authorization: Bearer $PLATFORM_TOKEN" -H "Content-Type: application/json" \
  -d '{"code":"c001","name":"某某学院","adminEmail":"admin@c001.edu","adminPassword":"","seedDemo":false}'
```

响应里的 `initialAdminPassword` 是该客户管理员的初始密码(只返回这一次,`adminPassword` 留空时随机生成),`siteHost` 是客户的访问地址 `c001.labcloud.com.cn`。整个开通过程:建库 → 执行全部 Flyway 脚本 → 创建管理员,约 1 秒。

其他平台接口(都带同一个 Bearer 令牌):`GET /api/platform/tenants` 列表;`PUT /api/platform/tenants/c001/status` body `{"status":"SUSPENDED"|"ACTIVE"}` 停用/恢复(停用后该站点所有请求返回 403);`DELETE /api/platform/tenants/c001?confirm=c001&dropData=true` 注销并删库删文件(不可恢复,不加 `dropData` 只从注册表移除、数据保留)。

**4)备份**:每个客户一个库,逐库导出即可按客户恢复或迁出:

```bash
for db in $(docker exec ioedu-mysql sh -c 'exec mysql -uroot -p"$MYSQL_ROOT_PASSWORD" -N -e "SHOW DATABASES LIKE '"'"'ioedu%'"'"'"'); do
  docker exec ioedu-mysql sh -c "exec mysqldump -uroot -p\"\$MYSQL_ROOT_PASSWORD\" --single-transaction $db" | gzip > backup_${db}_$(date +%F).sql.gz
done
```

上传文件在数据卷 `ioedu-uploads` 下按租户编码分目录(`/app/uploads/c001/...`),连同一起备份。

**5)资源与连接**:100 个客户共用一个后端进程与一个连接池(`DB_POOL_SIZE` 20~30 足够),推荐 8 核 16G;MySQL 的 `max_connections` 已在 compose 中设为 500。本地开发可用 dev profile + `X-Tenant-Id` 请求头切换租户,`smoke-test-tenant.ps1` 会自动开通一个临时租户跑完全部隔离检查再注销。

## 5.3 项目商店(hub):客户之间共享项目

`hub/` 是一个独立的 Spring Boot 服务(compose 中的 `ioedu-hub`,独立库 `ioedu_hub`,附件在数据卷 `ioedu-hub-assets`),所有客户站点共用。客户站点只通过自己的后端调用商店(浏览器不直连,API Key 不出服务端);平台管理员在 `https://www.根域/platform/login` 登录商店后台做审核与分享。

```
客户 A 站点后端 ──API Key A──┐
客户 B 站点后端 ──API Key B──┼──→ ioedu-hub(条目 / 版本 / 审核 / 定向分享 / 安装记录 / 附件)
平台管理端 /platform/** ─────┘      ↑ 平台管理员账号(HUB_ADMIN_*)
```

**1)`.env`**:`HUB_JWT_SECRET`(必填,≥32 随机字符)、`HUB_ADMIN_USERNAME` / `HUB_ADMIN_PASSWORD`(平台管理员,密码留空则随机生成并打印到 `docker compose logs hub`)、`MASTER_KEY`(客户后端加密保存 API Key 用,≥32 随机字符,**换了会导致已保存的密钥无法解密**)。

**2)客户接入**:通过「客户站点」页开通的站点会自动完成商店登记与密钥写入,无需人工操作。对于升级前已存在的站点(如默认站点)或密钥泄露时,在「客户站点」页点「重签商店密钥」,新密钥会自动写入该站点;若自动写入失败会显示密钥,交给客户管理员在其站点「管理后台 → 项目商店 → 接入设置」填入。商店服务通过 `HUB_PLATFORM_API_URL`(默认 `http://backend:8080`)与 `HUB_PLATFORM_TOKEN`(即 `PLATFORM_TOKEN`)调用站点后端。

**3)发布与安装**:客户侧有两条发布路径——管理员在「管理后台 → 项目商店 → 本站发布」的项目列表里对任意项目点「发布到商店」;教师在「教师工作台 → 我的项目」对自己指导的项目点「发布到商店」(教师只能发布/更新自己的项目)。项目的封面、富文本图片、教学资料会一并上传到商店并按内容去重。平台管理员在「条目审核与分享」审核通过后上架,可设为「所有客户可见」或「仅指定客户可见」,并在任意可见范围下追加定向分享名单;修改后再次发布会追加新版本重新审核,期间旧版本继续上架。其他客户在「浏览商店」安装,安装是**复制一份**到本站(附件下载到本站目录,教师安装的项目自动指派给自己),本地记住上游条目与版本号,商店有新版本时显示「有新版本」并可一键覆盖更新教学内容(讲师、报名、成果数据保留)。

**4)备份**:`ioedu_hub` 库与 `ioedu-hub-assets` 数据卷一并纳入 5.2 的备份脚本(库名 `ioedu%` 已包含它)。

## 5.4 AI 助手(SKILL / 工具 / 流式对话)

- 大模型接口按客户各自配置(管理后台「AI 设置」,Key 加密存库,需 `.env` 里的 `MASTER_KEY`),也可用 `AI_*` 环境变量给全部客户一个默认值;每客户独立并发闸与熔断,一个客户的 Key 失效不影响其他客户。
- 流式对话走 SSE:`frontend/nginx.conf` 已对 `/api/ai/` 关闭 `proxy_buffering` 并放宽超时;若前面还有别的反向代理(Caddy / 云负载均衡),同样需要关闭对该路径的缓冲。
- 配额:`AI_DAILY_RUNS`(每人每日运行次数,默认 50);工具级别的开关、角色、确认与每日次数由客户管理员在「AI 中心 → 工具策略」设置。
- 审计:每次运行与每次工具调用(含参数与结果摘要)都在客户库的 `ai_runs` / `ai_tool_invocations`,管理员可在「AI 中心 → 调用审计」查看。

## 6. 日常运维

```bash
# 查看状态 / 日志
docker compose ps
docker compose logs -f backend
docker compose logs -f frontend

# 重启某个服务
docker compose restart backend

# 更新发版(拉新代码后重建)
git pull
docker compose up -d --build

# 完全停止(不删数据)
docker compose down

# 数据库备份(生成 SQL 文件,建议配 crontab 每日执行)
docker exec ioedu-mysql sh -c 'exec mysqldump -uroot -p"$MYSQL_ROOT_PASSWORD" ioedu' > backup_$(date +%F).sql

# 数据库恢复
cat backup_2026-07-24.sql | docker exec -i ioedu-mysql sh -c 'exec mysql -uroot -p"$MYSQL_ROOT_PASSWORD" ioedu'
```

## 7. 绑定域名 + HTTPS(可选)

域名解析 A 记录指到服务器 IP 后,推荐用 [Caddy](https://caddyserver.com) 自动签发证书,最少改动:

1. frontend 的端口映射已是 `"8093:80"`,80/443 空闲,可直接给 Caddy 使用
2. 服务器安装 Caddy 并写 `/etc/caddy/Caddyfile`:

```
你的域名.com {
    reverse_proxy localhost:8093
}
```

3. `systemctl restart caddy`,访问 `https://你的域名.com`,证书自动签发续期。

也可以用宝塔面板 / 手动 certbot + nginx,原理相同:HTTPS 终止在外层,反代到 frontend 容器。

---

# 方式二:无 Docker 手动部署

适合内网机器或不方便装 Docker 的场景。

## 1. 环境准备

- JDK 8、MySQL 8、Nginx、Node 18+(仅构建用,可在本机构建后只传产物)

## 2. 数据库

```sql
CREATE DATABASE ioedu DEFAULT CHARACTER SET utf8mb4;
```

## 3. 后端

```bash
# 构建(本机或服务器)
mvn package -Dmaven.test.skip=true
# 产物: target/IOEDU-New-0.0.1-SNAPSHOT.jar

# 服务器上运行(密钥用环境变量注入;IOEDU_JWT_SECRET 必填且不少于 32 字符)
export IOEDU_DB_PASSWORD=你的密码
export IOEDU_JWT_SECRET=$(openssl rand -base64 48)
export IOEDU_ADMIN_PASSWORD=首个管理员密码   # 留空则随机生成并打印到日志
nohup java -jar IOEDU-New-0.0.1-SNAPSHOT.jar > ioedu.log 2>&1 &
```

建议注册成 systemd 服务实现开机自启:

```ini
# /etc/systemd/system/ioedu.service
[Unit]
Description=IOEDU Backend
After=mysql.service

[Service]
Environment=IOEDU_DB_PASSWORD=你的密码
Environment=IOEDU_JWT_SECRET=不少于32字符的随机串
Environment=IOEDU_ADMIN_PASSWORD=首个管理员密码
ExecStart=/usr/bin/java -jar /opt/ioedu/IOEDU-New-0.0.1-SNAPSHOT.jar
Restart=always

[Install]
WantedBy=multi-user.target
```

```bash
systemctl daemon-reload && systemctl enable --now ioedu
```

## 4. 前端

```bash
cd frontend && npm install && npm run build
# 把 dist/ 上传到服务器,例如 /opt/ioedu/dist
```

Nginx 配置(`/etc/nginx/conf.d/ioedu.conf`):

```nginx
server {
    listen 80;
    server_name _;

    location /api/ {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location / {
        root /opt/ioedu/dist;
        index index.html;
        try_files $uri $uri/ /index.html;   # SPA 路由回退,必须有
    }
}
```

```bash
nginx -t && systemctl reload nginx
```

---

# 常见问题

| 现象 | 排查 |
|---|---|
| backend 容器反复重启 | `docker compose logs backend`;多为 MySQL 未就绪(healthcheck 会自动等)或 .env 密码与已有数据卷密码不一致。改过密码需 `docker compose down -v` 清卷重建(会清空数据!) |
| 启动报 "JWT 密钥未配置或过短" | `.env` 里没填 `JWT_SECRET` 或少于 32 字符;手动部署则是没有 `export IOEDU_JWT_SECRET` |
| 启动报 Flyway 错误 | 某个租户库的迁移失败:看日志里是哪个库,确认所有 `db/migration` 脚本都已随代码一起部署;手工改过表结构的库需按报错提示对齐 |
| 访问返回 `{"code":404,"message":"站点不存在"}` | Host 没有匹配到任何租户且 `TENANT_STRICT_HOST=true`;检查泛域名解析与租户编码,或临时改为 false |
| 登录后提示「令牌与当前站点不匹配」 | 用了另一个客户站点签发的令牌(或升级前的旧令牌),重新登录即可 |
| 平台接口返回 403「未启用」 | `.env` 没配 `PLATFORM_TOKEN` 或少于 16 位 |
| 前端构建卡在 npm install | 编辑 `frontend/Dockerfile`,取消 npmmirror 注释 |
| 8093 端口被占用 | `docker-compose.yml` 中 frontend 端口改为如 `"8094:80"`,访问 http://IP:8094 |
| 页面能开但接口 502 | backend 未启动完成,等 healthcheck 通过;`curl http://localhost:8093/api/public/stats` 验证 |
| 刷新子页面 404 | Nginx 缺 `try_files ... /index.html`(Docker 方案已内置) |
| 忘记管理员密码 | 数据库执行:`UPDATE users SET password_hash='$2a$10$N9qo8uLOickgx2ZMRZoMyeIjZAgcfl7p92ldGxad68LJZdL17lhWy' WHERE email='admin@ioedu.cn';`(重置为 `secret`,登录后尽快修改) |

---

# 自动部署(GitHub Actions)

push 到 main 分支时,`.github/workflows/deploy.yml` 会通过 SSH 登录服务器执行 `git pull` + `docker compose up -d --build`(也可在 Actions 页面手动触发)。启用前提:

1. 服务器已按方式一完成首次部署,代码位于 `/opt/ioedu`(即 `cd /opt && git clone <仓库地址> ioedu`),且该目录能免交互执行 `git pull`
2. 在 GitHub 仓库 Settings → Secrets and variables → Actions 中配置三个 Secrets:
   - `SERVER_HOST`:服务器 IP 或域名
   - `SERVER_USER`:SSH 登录用户(如 root)
   - `SERVER_SSH_KEY`:该用户的 SSH 私钥(对应公钥需在服务器 `~/.ssh/authorized_keys` 中)
