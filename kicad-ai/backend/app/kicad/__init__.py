"""Lightweight KiCad file support (S-expression parsing, PCB / schematic helpers).

This module deliberately has zero third-party dependencies so the assistant can
run in "demo mode" without KiCad, `kicad-cli` or the upstream `kcaa` MCP server.
"""
