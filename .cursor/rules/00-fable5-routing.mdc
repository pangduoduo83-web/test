---
description: Fable 5 routing policy — always delegate code writing/modification/review/debugging/testing to the pinned Fable 5 thinking max subagents, and sign off with the executor's name at the end of every completed task
alwaysApply: true
---

# Fable 5 委派与署名总规则（本仓库默认约定）

本仓库（IOEDU-New 设备图书馆，Spring Boot 2.6 + Vue 3 全栈）约定：**所有实现类与审查类工作默认由 Fable 5 thinking max 执行**。规则本身无法在平台层切换主会话模型，因此正确做法是：父代理遇到下表中的任务时，**主动委派**到 `.cursor/agents/` 下已用 `model: claude-fable-5[effort=xhigh]` 钉死模型的子代理。

## 路由表（何时委派给谁）

| 任务类型 | 委派目标子代理 | 说明 |
|---|---|---|
| 写新代码 / 实现功能 / 改代码 / 修 bug / 重构 | `fable5-code-writer` | 任何会产生代码 diff 的工作 |
| 代码审查 / PR 审查 / diff 评估 / 安全检查 | `fable5-code-reviewer` | 只读审查，不改代码 |
| 排查报错 / 定位缺陷 / 分析异常日志 | `fable5-debugger` | 先诊断根因，修复动作可再交给 code-writer 或经确认后自行小修 |
| 写测试 / 冒烟验证 / 扩展 smoke-test.ps1 | `fable5-tester` | 本项目测试薄弱，新增行为尽量补验证 |
| 文档、PR 说明、提交信息等围绕代码变更的写作 | 随所属主任务的子代理一并完成 | 例如实现后由 code-writer 顺带写 PR 说明 |

## 例外（可不委派的情形）

- 纯问答、解释概念、不改代码的代码库探索与阅读，父代理可自行完成。
- 但只要用户**明确要求**改代码、审查代码，或任务实质上需要产生/评审 diff，就必须委派到对应子代理，不得以「改动很小」为由跳过。

## 署名要求（强制）

任何一次任务完成后，最终回复末尾必须单独一行输出实际执行者，固定格式：

```
✅ 本任务由 <子代理中文名>（<name>）· Fable 5 thinking max 完成
```

例如：`✅ 本任务由 Fable 5 代码工匠（fable5-code-writer）· Fable 5 thinking max 完成`

- 若父代理未委派而自行完成了实现/审查，也必须如实署名，例如：`✅ 本任务由主代理按 Fable 5 约定完成（未委派子代理）`。
- 署名必须真实反映执行者，禁止虚构。多个子代理协作时逐一列出。
